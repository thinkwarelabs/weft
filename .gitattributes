# Normalize line endings to LF in the repo (silences CRLF churn on Windows).
* text=auto eol=lf

# Binary — never touch.
*.ttf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.ico   binary
*.pdf   binary
